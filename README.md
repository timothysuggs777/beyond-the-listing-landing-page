# Beyond the Listing Static Build

This package contains a finished static HTML/CSS implementation intended to be imported into Bolt with minimal interpretation.

Start with:
- `index.html`
- `styles.css`
- `prompt/bolt-import-prompt.txt`

Use the import prompt in Bolt and instruct it to preserve the static implementation.
